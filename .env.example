# Go API server address.
MIRRORSELF_HTTP_ADDR=:3001

# PocketBase bind address. Keep it on loopback when it runs in the same process.
MIRRORSELF_POCKETBASE_ADDR=127.0.0.1:8090

# Optional outbound notification endpoint. Leave empty to disable notifications.
MIRRORSELF_NOTIFY_URL=
